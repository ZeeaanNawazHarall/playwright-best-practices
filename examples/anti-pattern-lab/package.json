{
  "name": "anti-pattern-lab",
  "version": "1.0.0",
  "description": "Reference code — not runnable. Install to get TypeScript type resolution in your IDE.",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
